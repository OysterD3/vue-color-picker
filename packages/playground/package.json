{
  "name": "playground",
  "scripts": {
    "dev": "vite",
    "prebuild": "npx pnpm install -r --store=node_modules/.pnpm-store || echo skipping pnpm install",
    "build": "vite build"
  },
  "dependencies": {
    "@oysterlee/vue-color-picker": "workspace:*"
  }
}
